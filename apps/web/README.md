# Web App (Planned)

This package will host the Next.js PWA application using shadcn/ui.

Planned setup order:
1. Initialize Next.js app structure.
2. Install and configure shadcn/ui.
3. Configure `next-pwa` for offline shell and cache strategy.
4. Build MVP screens: Home, Study, Review, Sprint, Collection.
